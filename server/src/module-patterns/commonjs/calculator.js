var advancedMath = require('./advancedMath.js');

console.log("result: " + advancedMath.multThenSum(1,2,3));


// within node, we takes care of the module loading - all we have to provide is a commonJS module format, node can work with